{
  "dependencies": {
    "inquirer": "^8.0.0",
    "inquirer-date-prompt": "^2.0.0"
  },
  "name": "shopee-developers-case",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build",
    "start": "clear && yarn clean && yarn build && node build/index.js"
  },
  "devDependencies": {
    "@types/inquirer": "^7.3.1"
  }
}
